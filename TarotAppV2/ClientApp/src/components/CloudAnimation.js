import React from 'react';
import './CloudAnimation.css'; // Create a CSS file for styling

const CloudAnimation = () => {
    return (
        <div className="cloud-container">
            <div className="cloud"></div>
        </div>
    );
};

export default CloudAnimation;
